<nav class="navbar">
  <div class="navbar-container">
    <div class="navbar-brand">
      <a href="/listings" class="logo"> <i class="fa-solid fa-web-awesome"></i> HealthWay Travels</a>
    </div>
    
    <div class="navbar-menu">
      <ul class="navbar-links">
        <li><a href="/listings" class="nav-link">All Listings</a></li>
        <li><a href="/listings/new" class="nav-link">Add Listing</a></li>
        
        <% if (typeof session !== 'undefined' && session.userId) { %>
          <!-- Logged-in user menu -->
          <li class="nav-item">
            <span class="user-name">👤 <%= session.userName || session.userEmail %></span>
          </li>
          <li><a href="/logout" class="nav-link btn-logout">Logout</a></li>
        <% } else { %>
          <!-- Non-logged-in user - Single "Get Started" button -->
          <!-- <li><a href="/signup" class="nav-link btn-getstarted">Get Started</a></li> -->
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<style>
  .navbar {
    background-color: #4b6cb7; /* simple blue shade */
    padding: 10px 20px;
    position: sticky; /* makes navbar stay at the top */
    top: 0;
    z-index: 1000; /* ensures it stays above other elements */
  }

  .navbar-container {
    max-width: 1100px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .logo {
    font-size: 22px;
    font-weight: bold;
    color: white;
    text-decoration: none;
  }

  .navbar-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
  }

  .nav-link {
    color: white;
    text-decoration: none;
    font-size: 15px;
  }

  .nav-link:hover {
    text-decoration: underline;
  }

  .user-name {
    color: #fff;
    font-size: 14px;
    font-weight: 600;
  }

  .btn-getstarted {
    background-color: white;
    color: #4b6cb7;
    padding: 8px 14px;
    border-radius: 4px;
    font-weight: 600;
    text-decoration: none;
  }

  .btn-getstarted:hover {
    background-color: #e0e0e0;
  }

  .btn-logout {
    background-color: #ff4d4d;
    color: white;
    padding: 8px 14px;
    border-radius: 4px;
    font-weight: 600;
    text-decoration: none;
  }

  .btn-logout:hover {
    background-color: #e63946;
  }

  /* Responsive design for small screens */
  @media (max-width: 768px) {
    .navbar-container {
      flex-direction: column;
      text-align: center;
    }

    .navbar-links {
      flex-direction: column;
      gap: 10px;
      margin-top: 10px;
    }
  }
</style>
